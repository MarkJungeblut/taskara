# dashboard-editor feature

Purpose: own the dashboard draft state and editor UI for filters, columns, and sorting.

Public API:
- `DashboardApp`
- `createEmptyDashboard()`
- `ensureColumns()`

Dependencies:
- `frontend/features/workspace`
- `frontend/features/dashboard-files`
- `frontend/features/query-preview`
- `frontend/shared/*`
